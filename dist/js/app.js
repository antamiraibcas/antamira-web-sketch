// // Declare HTML document parts
// const visualizer = document.getElementById('visualizer');
// const img = document.querySelectorAll('.box');
// const textCarousel = document.querySelector('.text-carousel');
// // Declare index
// let idx=0;

// function run(){
//     idx++;
//     if(idx == img.length){
//         idx = 0;
//     }
//     visualizer.style.transform = `translateX(${-idx * 426}px)`;
//     textCarousel.style.transform = `translateX(${-idx * 426}px)`;
// }

// setInterval(run, 10000);

